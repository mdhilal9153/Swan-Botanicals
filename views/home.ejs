<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Swan Botanicals</title>
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Open+Sans:wght@400;600&display=swap" rel="stylesheet">
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <style>
        /* Define Brand Colors */
        :root {
            --forest-canopy: #2A4D3A;
            --warm-ivory: #F8F4E3;
            --botanical-charcoal: #2C2C2A;
        }

        body {
            font-family: 'Open Sans', sans-serif;
        }

        /* Custom styles for the navbar */
        .navbar {
            padding-top: 1rem;
            padding-bottom: 1rem;
            background-color: #fff !important;
        }
        .navbar-brand {
            font-family: 'Open Sans', sans-serif;
            font-weight: 600;
            letter-spacing: 1px;
            text-transform: uppercase;
        }
        .navbar-nav .nav-link {
            text-transform: uppercase;
            font-size: 0.9rem;
            font-weight: 600;
            letter-spacing: 0.5px;
            color: var(--botanical-charcoal);
            margin: 0 1rem;
        }
        .navbar-nav .nav-link.active,
        .navbar-nav .nav-link:hover {
            color: #000;
            border-bottom: 2px solid #000;
            padding-bottom: 4px;
        }
        .navbar-icons a {
            color: var(--botanical-charcoal);
            font-size: 1.2rem;
            margin-left: 1.5rem;
        }
        .navbar-icons a:hover {
            color: #000;
        }
        @media (min-width: 992px) {
            .navbar-nav {
                position: absolute;
                left: 50%;
                transform: translateX(-50%);
            }
        }

        /* Custom styles for the hero slideshow */
        #heroCarousel {
            width: 100%;
            /* Using aspect-ratio for a consistent rectangular shape. 16/7 is a wide, panoramic ratio. */
            aspect-ratio: 16 / 7;
            /* Set a max-height to prevent it from becoming overly tall on extremely wide screens */
            max-height: 600px;
            /* Override any potential min-height from Bootstrap's CSS */
            min-height: auto !important;
        }

        .carousel-item {
            /* Ensure the carousel item fills the new aspect-ratio container */
            height: 100%;
        }

        .carousel-item img {
            /* This ensures the image covers the entire area of the container without distortion */
            object-fit: cover;
            height: 100%;
            width: 100%;
            filter: brightness(0.7); /* Darken image slightly to make text pop */
        }
        
        .carousel-caption {
            /* Override bootstrap defaults */
            right: 0;
            left: 0;
            bottom: 0;
            top: 0;
            padding: 2rem;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            color: #fff;
        }
        .carousel-caption h1 {
            font-family: 'Playfair Display', serif;
            font-size: clamp(2.5rem, 5vw, 4.5rem); /* Responsive font size */
            font-weight: 700;
            max-width: 800px;
        }
        .carousel-caption p {
            font-family: 'Open Sans', sans-serif;
            font-size: clamp(1rem, 2vw, 1.25rem);
            max-width: 600px;
            margin-top: 1rem;
        }
        .carousel-caption .btn-ritual {
            background-color: var(--warm-ivory);
            color: var(--forest-canopy);
            border: none;
            padding: 0.8rem 2.5rem;
            font-family: 'Open Sans', sans-serif;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 1px;
            border-radius: 50px;
            margin-top: 2rem;
            transition: all 0.3s ease;
        }
        .carousel-caption .btn-ritual:hover {
            background-color: #fff;
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0,0,0,0.2);
        }
        .carousel-indicators [data-bs-target] {
            width: 10px;
            height: 10px;
            border-radius: 100%;
            margin: 0 5px;
        }

        /* Styles for the Products Section */
        .products-section {
            padding: 5rem 0; /* Add generous top and bottom padding */
        }
        .products-section h2 {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            color: var(--botanical-charcoal);
        }
        .product-card {
            position: relative; /* Needed for the overlay */
            border-radius: 8px;
            overflow: hidden; /* Keeps the overlay within the rounded corners */
            cursor: pointer;
            background-color: #fff;
        }
        .product-card img {
            max-width: 100%;
            height: auto;
            display: block; /* Removes any extra space below the image */
        }
        .product-overlay {
            position: absolute;
            top: 0;
            bottom: 0;
            left: 0;
            right: 0;
            height: 100%;
            width: 100%;
            opacity: 0; /* Hidden by default */
            transition: .3s ease;
            background-color: rgba(42, 77, 58, 0.85); /* Semi-transparent Forest Canopy */
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 1.5rem;
        }
        .product-card:hover .product-overlay {
            opacity: 1; /* Show on hover */
        }
        .product-overlay h5 {
            color: #fff;
            font-family: 'Playfair Display', serif;
        }
        .product-overlay p {
            color: var(--warm-ivory);
        }

    </style>
</head>
<body>

    <!-- Navbar Section -->
    <nav class="navbar navbar-expand-lg border-bottom sticky-top">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">Swan Botanicals</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
          <ul class="navbar-nav mb-2 mb-lg-0">
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="#">Shop</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Our Story</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">The Botanical Journal</a>
            </li>
          </ul>
          <div class="d-flex align-items-center navbar-icons">
            <a href="#" aria-label="Search"><i class="bi bi-search"></i></a>
            <a href="#" aria-label="User Account"><i class="bi bi-person"></i></a>
            <a href="#" aria-label="Shopping Cart"><i class="bi bi-bag"></i></a>
          </div>
        </div>
      </div>
    </nav>

    <!-- Hero Slideshow Section -->
    <div id="heroCarousel" class="carousel slide" data-bs-ride="carousel">
      <!-- Indicators/dots -->
      <div class="carousel-indicators">
        <button type="button" data-bs-target="#heroCarousel" data-bs-slide-to="0" class="active" aria-current="true" aria-label="Slide 1"></button>
        <button type="button" data-bs-target="#heroCarousel" data-bs-slide-to="1" aria-label="Slide 2"></button>
      </div>
    
      <!-- The slideshow/carousel -->
      <div class="carousel-inner">
        <div class="carousel-item active">
          <img src="https://images.unsplash.com/photo-1751283246779-cb72aa04250b?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" class="d-block w-100" alt="Serene woman with eyes closed surrounded by leaves">
          <div class="carousel-caption">
            <h1>Botanical beauty, inspired by a mother's love</h1>
            <p>Gentle care, visible results. 99% botanical, 0% synthetic chemicals.</p>
            <button class="btn btn-ritual">Discover Your Ritual</button>
          </div>
        </div>
        <div class="carousel-item">
          <img src="https://images.unsplash.com/photo-1748279944004-f1d733dc711b?q=80&w=2070&auto=format&fit=crop&ixlib=rb-4.1.0&ixid=M3wxMjA3fDB8MHxwaG90by1wYWdlfHx8fGVufDB8fHx8fA%3D%3D" class="d-block w-100" alt="Skincare products in a beautiful flatlay with botanical elements">
           <div class="carousel-caption">
            <h1>Pure Ingredients, Powerful Results</h1>
            <p>Experience the difference of nature's finest, crafted with care.</p>
            <button class="btn btn-ritual">Shop Best Sellers</button>
          </div>
        </div>
      </div>
    
      <!-- Left and right controls/icons -->
      <button class="carousel-control-prev" type="button" data-bs-target="#heroCarousel" data-bs-slide="prev">
        <span class="carousel-control-prev-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Previous</span>
      </button>
      <button class="carousel-control-next" type="button" data-bs-target="#heroCarousel" data-bs-slide="next">
        <span class="carousel-control-next-icon" aria-hidden="true"></span>
        <span class="visually-hidden">Next</span>
      </button>
    </div>


    <!-- Featured Products Section -->
    <div class="products-section">
        <div class="container">
            <!-- Section Title -->
            <div class="row mb-5 text-center">
                <div class="col">
                    <h2>Featured Botanicals</h2>
                    <p class="lead text-muted">Hand-picked selections for your daily ritual.</p>
                </div>
            </div>

            <!-- Product Grid -->
            <div class="row">
                <!-- Product 1 -->
                <div class="col-md-4 mb-4">
                    <div class="product-card">
                        <img src="https://placehold.co/400x400/F8F4E3/2A4D3A?text=Product+One" class="img-fluid" alt="Product Image">
                        <div class="product-overlay">
                            <h5 class="fw-bold">Flawless Facial Oil</h5>
                            <p>A rejuvenating blend of natural oils.</p>
                        </div>
                    </div>
                </div>
                <!-- Product 2 -->
                <div class="col-md-4 mb-4">
                    <div class="product-card">
                        <img src="https://placehold.co/400x400/F8F4E3/2A4D3A?text=Product+Two" class="img-fluid" alt="Product Image">
                        <div class="product-overlay">
                            <h5 class="fw-bold">Hydrating Mist</h5>
                            <p>Instant refreshment for tired skin.</p>
                        </div>
                    </div>
                </div>
                <!-- Product 3 -->
                <div class="col-md-4 mb-4">
                    <div class="product-card">
                        <img src="https://placehold.co/400x400/F8F4E3/2A4D3A?text=Product+Three" class="img-fluid" alt="Product Image">
                        <div class="product-overlay">
                           <h5 class="fw-bold">Nourishing Cream</h5>
                           <p>Rich, deep moisture for a healthy glow.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>


    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
